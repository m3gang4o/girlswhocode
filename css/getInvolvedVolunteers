* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
}

body {
    background-color: #f5f5f5;
}

.hero-section {
    background-color: #e0f2f1;
    padding: 4rem 1rem; /* reduced side padding */
    text-align: center;
    margin-bottom: 2rem;
}

.hero-text {
    max-width: 1200px; /* increased from 800px to stretch the text more */
    margin: 0 auto 2rem auto;
    line-height: 1.6;
    color: #333;
    padding: 0 1rem; /* added small padding for mobile */
    font-size: 22px;
    padding-bottom: 50px;
}

.hero-content h2 {
    color: #0D9C90;
    margin-bottom: 1.5rem;
    font-size: 2.5rem;
    padding-bottom: 25px;
}


.apply-btn {
    background-color: white;
    border: none;
    padding: 1.2rem 7rem; /* increased padding */
    border-radius: 30px; /* slightly increased border radius */
    font-size: 1.4rem; /* increased font size from 1.1rem */
    cursor: pointer;
    color: #0D9C90;
    transition: all 0.3s ease;
    margin-top: 1rem; /* optional: adds some space above the button */
    text-decoration: none;
}

.apply-btn:hover {
    background-color: #006d77;
    color: white;
    transform: scale(1.05); /* optional: slight grow effect on hover */
}

.role-title {
    padding-bottom: 50px;
    padding-top: 40px;
}
.roles-section {
    padding: 2rem;
    max-width: 1200px;
    margin: 0 auto;
}

.roles-section h2 {
    color: #0D9C90;
    text-align: center;
    margin-bottom: 2rem;
    font-size: 3rem;
}

.role-cards {
    display: flex;
    flex-direction: column;
    gap: 3.5rem;
}

.role-card {
    display: flex;
    background-color: #e0f2f1;
    border-radius: 10px;
    overflow: hidden;
    padding: 2rem;
    gap: 2rem;
}


.role-card:nth-child(2) {
    flex-direction: row-reverse;
}

.role-text {
    flex: 2;
}

.role-text h3 {
    color: #0D9C90;
    margin-bottom: 2rem;
    font-size: 30px;
}

.role-text ul {
    list-style-position: inside;
    list-style-type: none;
    color: #333;
    font-size: 1.2rem
}

.role-text li {
    margin-bottom: 0.5rem;
    line-height: 1.4;
    position: relative;
    padding-left: 1.5em;
}

.role-text li::before {
    content: "⭐"; /* custom bullet */
    /* #0D9C90 */
    position: absolute;
    left: -0.5em;
    top: 0;
    color: #333;
    background: linear-gradient(#0D9C90, #0D9C90);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    
}


.role-image {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
}

.role-image img {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
}

@media (max-width: 768px) {
    .role-card {
        flex-direction: column;
    }
    
    .hero-content h2 {
        font-size: 1.5rem;
    }
    
    .hero-text {
        font-size: 0.9rem;
    }
    .apply-btn {
        padding: 1rem 2.5rem; /* slightly smaller padding on mobile */
        font-size: 1.2rem; /* slightly smaller font on mobile */
    }
    .role-text h3 {
        font-size: 1.6rem; /* slightly smaller on mobile but still prominent */
        margin-bottom: 1rem;
    }

    .role-text li {
        text-align: left;
        padding-left: 2rem; /* increased space for better alignment */
        text-indent: -1.5rem; /* creates hanging indent for wrapped lines */
        margin-left: 1.5rem; /* offset the negative text-indent */
    }

    .role-card, 
    .role-card:nth-child(2) {
        flex-direction: column; /* Stack vertically on mobile regardless of card */
    }
}
